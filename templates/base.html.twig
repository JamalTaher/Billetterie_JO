<!DOCTYPE html>
<html style="height: 100%;">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg viewBox='0 0 24 24' xmlns='http://www.w3.org/2000/svg'><path fill='currentColor' d='M12 16.667c-2.583 0-4.667-2.083-4.667-4.667 0-2.583 2.084-4.667 4.667-4.667 2.583 0 4.667 2.084 4.667 4.667 0 2.584-2.084 4.667-4.667 4.667zM12 24c-4.418 0-8-3.582-8-8v-8c0-4.418 3.582-8 8-8s8 3.582 8 8v8c0 4.418-3.582 8-8 8z'/></svg>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
        <style>
            .footer-small-text {
                font-size: 0.9rem;
            }
        </style>
    {% endblock %}
</head>
<body style="display: flex; flex-direction: column; min-height: 100vh; margin: 0;">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_home') }}">Accueil</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_offres') }}">Nos Offres</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_panier_voir') }}">
                            Panier <span id="panier-count" class="badge bg-secondary">{{ panier_count|default(0) }}</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ asset('documents/notice_utilisation.pdf') }}" target="_blank">Aide</a>
                    </li>
                </ul>
                <ul class="navbar-nav ms-auto">
                    {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_profile_edit') }}">Modifier mes informations</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_profile_change_password') }}">Modifier mon mot de passe</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_deconnexion') }}">Déconnexion</a>
                        </li>
                        {% if is_granted('ROLE_ADMIN') %}
                            {# LIEN VERS LE TABLEAU DE BORD ADMIN GÉNÉRAL #}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('admin_index') }}">Admin (Tableau de bord)</a> {# Le href 'admin_index' va maintenant vers le dashboard #}
                            </li>
                            {# LIEN VERS LA GESTION DES ÉVÉNEMENTS SPÉCIFIQUEMENT #}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('admin_select_category') }}">Admin (Gérer Événements)</a> {# NOUVEAU LIEN ET LABEL #}
                            </li>
                            {# LIEN POUR GÉRER LES OFFRES (déjà ajouté) #}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_offre_index') }}">Admin (Gérer Offres)</a> {# MODIFIÉ le label pour cohérence #}
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('admin_stats_index') }}">Statistiques</a>
                            </li>
                        {% endif %}
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4" style="flex-grow: 1;">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
        {% block body %}{% endblock %}
    </div>

    <footer class="py-4 mt-5 footer-small-text" style="background-color: #1f2945; color: white;">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h5>Jeux Olympiques de Paris 2024</h5>
                    <p>Organisateur: Comité d'Organisation des Jeux Olympiques de Paris 2024 </p>
                </div>
                <div class="col-md-4">
                    <h5>Contact</h5>
                    <p>Email: support@jo2024-simulation.com</p>
                </div>
                <div class="col-md-4">
                    <h5>Informations Utiles</h5>
                    <ul class="list-unstyled">
                        <li style="color: white;">CGV (Simulation)</li>
                        <li style="color: white;">Politique de Confidentialité (Simulation)</li>
                    </ul>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

    <script src="{{ asset('js/panier.js') }}"></script>
    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</body>
</html>