{% extends 'admin/index.html.twig' %} {# CHANGEMENT ICI : 'base.html.twig' devient 'admin/index.html.twig' #}

{% block title %}Offre{% endblock %}

{% block body_content %} {# CHANGEMENT ICI : 'body' devient 'body_content' #}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ offre.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offre.description }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ offre.capacite }}</td>
            </tr>
            <tr>
                <th>PrixStandard</th>
                <td>{{ offre.prixStandard }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offre_index') }}" class="btn btn-secondary mt-3">back to list</a> {# Ajout de classe pour le style #}

    <a href="{{ path('app_offre_edit', {'id': offre.id}) }}" class="btn btn-warning mt-3">edit</a> {# Ajout de classe pour le style #}

    {{ include('offre/_delete_form.html.twig') }} {# Aucune modification nécessaire ici #}
{% endblock %}