{% extends 'base.html.twig' %}

{% block title %}Accueil - Jeux Olympiques France{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Bienvenue aux Jeux Olympiques France !</h1>
        <p>Découvrez l'excitation des Jeux et réservez vos billets dès maintenant.</p>

        <h2>Aperçu des épreuves</h2>
        <div class="row">
            {% for offre in offres %}
                <div class="col-md-4 mb-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">{{ offre.nom }}</h5>
                            <p class="card-text">{{ offre.description|default('Pas de description pour le moment.') }}</p>
                            <p class="card-text">Prix : {{ offre.prix }} €</p>
                            <p class="card-text">Capacité : {{ offre.capacite }} personne{{ offre.capacite > 1 ? 's' : '' }}</p>
                            <a href="{{ path('app_offres') }}" class="btn btn-primary">Voir l'offre</a>
                        </div>
                    </div>
                </div>
                {% if loop.index % 3 == 0 %}</div><div class="row">{% endif %}
            {% endfor %}
        </div>

        <p class="mt-3"><a href="{{ path('app_offres') }}">Voir toutes les offres disponibles</a></p>
    </div>
{% endblock %}