{% extends 'admin/index.html.twig' %}

{% block body_content %}
    <h1>Modifier l'offre "{{ offre.nom }}"</h1>

    {{ form_start(form, {'attr': {'class': 'mb-3'}}) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Modifier l'offre</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_offres_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}